# Dev and CI-only dependencies — NOT installed in Docker image
# Install locally or in CI with: pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Testing
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0

# Linting
ruff==0.8.6

# Security scanning (CI only)
pip-audit==2.7.3
